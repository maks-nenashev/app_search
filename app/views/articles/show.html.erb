<%# provide :page_title, 'Article has been created!!!' %> <!Podpis w ikonke "Kakaja stranica">

<!3 Wywodim bazu po ID>
<h3>Thanks You!</h3>
<h4>Article has been created!!!</h4>

 <p class="mb-2"><%= @article.title %></p>
 <%= image_tag @article.image, style: "width: 380px; height: auto", class: 'img-thumbnail thumb'%>
 <br>
 <p class="lead my-3"><%= @article.text %></p>

             <! Podkluchenie time pod soobzheniem!>
  <time datetime="<%= @article.created_at.strftime("%Y-%m-%d %H:%M:%S")%>">
  <%= @article.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
  </time>  
   <%= link_to ('Edit'), edit_article_path(@article),class: "btn btn-secondary btn-sm"%> 
   <%= link_to ('Delete'), article_path(@article), data: {'turbo-method': :delete,'turbo-confirm': 
     'Are you sure?'}, class:"btn btn-danger btn-sm"%>
   <%= link_to "Показати всi оголошення!",articles_path, class:"button is-danger is-inverted" %> <!Prawelnaja ssylka>
<!/////////////////////////////////////////////////////////////////////////////////////////>

 <hr class="my-3">
<h4> Write youre Comment </h4>

<a class="button big  bi bi-pencil-square" href="#collapseExample" role="button" data-bs-toggle="collapse" aria-expanded="false">
     <%= ('Коментувати!')%>
  </a>
                     <!Dropdown Topic>
  <ul class="collapse" id="collapseExample"  data: {'bs-toggle': 'collapse'}>
                      <!Dobawil Comment>
 
 <%= form_with model: [@commentable, @comment] do |f| %>

  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
      <%= f.label :body %>
    </div>

    <div class="col-sm-10">                     <! Walidacija CSS ---required: true, minlength:2,>
      <%= f.text_area :body, class: 'form-control', required: true, minlength:2%>
    </div>
  </div>

  <%= f.submit "Submit comment!", class: 'btn btn-primary' %>
 <% end %>
 </ul>
<!/////////////////////////////////////////////////////////////////////////////////////>

<h4 class="mt-5">Comment!</h4>

<% @commentable.comments.each do |comment| %>
  <article class="mb-3 card" id="comment-<%= comment.id %>">
      <div class="card-body">
           <! Podkluchenie time pod soobzheniem!>
  <time datetime="<%= @article.created_at.strftime("%Y-%m-%d %H:%M:%S")%>">
  <%= @article.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
  </time>  
         
         <section class="card-text">
                  <p>
                    <%= comment.body %>
                  </p>
         </section>
              
 
 <%= link_to  "Edit article", edit_article_comment_path(@article,comment),class: "btn btn-secondary btn-sm"%> 
 <%= link_to 'Delete', article_comment_path(@article, comment), class: 'btn btn-danger btn-sm',
      data: {method: :delete, confirm: "Are you sure?"} %>
   </div>
 </article>
<%end%>